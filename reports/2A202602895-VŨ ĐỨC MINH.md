# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: VŨ ĐỨC MINH
- Mã học viên: 2A202602895
- Nhóm: ThreeMenSquad
- Repository/branch: local (thao tác không trên version control)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 5 — semantic search | Dense Chroma query, cosine score conversion and result contract | `src/task5_semantic_search.py` | Done |
| Task 6 — lexical search | Unicode tokenization and deterministic BM25 ranking | `src/task6_lexical_search.py` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Dense search trả score `1 - cosine distance` cùng metadata contract.  
   **Lý do/evidence:** Thống nhất interface với lexical và reranker.  
   **Trade-off:** Phụ thuộc embedding model đã cấu hình.

2. **Quyết định:** BM25 tie-break theo ID và fallback top-k khi toàn bộ score bằng 0.  
   **Lý do/evidence:** Kết quả tái lập được, không trả rỗng với corpus nhỏ.  
   **Trade-off:** Fallback có thể kém liên quan khi query không match.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `python -m pytest tests/test_contracts.py -q`.
- Kết quả trước/sau nếu có: contract tests pass; dense, BM25 và ordering được kiểm tra.
- Lỗi đã phát hiện và cách xử lý: không phụ thuộc `collection.count()` để tương thích collection giả trong test.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: BM25 khởi tạo corpus theo process và chưa có disk cache.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: benchmark hybrid theo từng nhóm tài liệu.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-21
- Tên thành viên: VŨ ĐỨC MINH
